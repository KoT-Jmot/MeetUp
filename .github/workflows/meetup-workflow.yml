name: meetup-actions-workflow
on:
  push:
    branches: 
      - KoT-Jmot-patch-1
      
jobs:
  meetup-testing:
    runs-on: ubuntu-latest
    steps:
    - name: Test forst jobs
      run: echo "Hello world!"
    
  
