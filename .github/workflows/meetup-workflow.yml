name: meetup-actions-workflow

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        microservice:
          - MeetUp.CommentsService
          - MeetUp.Gateway
          - MeetUp.HangFireSerivce
          - MeetUp.IdentityService
          - MeetUp.ЕventsService

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup .NET Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '7.0.100'

      - name: Restore dependencies
        run: |
          ${{ matrix.microservice }}
          dotnet restore

      - name: Build and test
        run: |
          ${{ matrix.microservice }}
          dotnet build --configuration Release --no-restore
          dotnet test --configuration Release --no-build
